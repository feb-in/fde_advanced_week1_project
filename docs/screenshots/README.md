# Screenshots / Evidence

Drop the PNGs here with these exact filenames — the README's "Screenshots / Evidence"
section embeds them by name, so they render automatically once added.

| Filename | What to capture |
|---|---|
| `ui-truth-vs-prediction.png` | The Streamlit UI (`http://localhost:8501`) after **Load random patient** — the truth-vs-prediction card with the ✓/✗ verdict, the risk %, the flag, and the top-factors chart. |
| `grafana-dashboard.png` | The Grafana dashboard (`http://localhost:3000` → *"Readmission API — Observability"*) with the panels populated — request rate, p50/p95 latency, error rate, total predictions. Send a little `/predict` traffic first so the panels have data. |
| `ci-green.png` | A green run of the **CI — test, build, push to ECR** workflow in the repo's GitHub **Actions** tab (all steps ✓). |
| `evidently-drift.png` | The Evidently report `reports/monitoring/drift_shifted.html` (generated by `uv run python src/monitoring/drift.py`) showing dataset drift detected on the shifted batch. |

How to produce the running surfaces: see the README **"How to see it live"** section.
